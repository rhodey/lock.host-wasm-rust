package local:app;

interface helpers-interface {
  chat-completion: async func(api-key: string, json: string) -> string;
  get-balance: async func(rpc: string, address: string) -> string;
}

world helpers {
  export helpers-interface;
}

world app {
  import helpers-interface;
}
